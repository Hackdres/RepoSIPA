{% extends 'base.html.twig' %}

{% block body %}
    <h1>Prodflias</h1>

    <table>
        <tbody>
            <tr>
                <th>Idprodflia</th>
                <td>{{ prodflia.idprodflia }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_prodflias_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_prodflias_edit', { 'id': prodflia.idprodflia }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
